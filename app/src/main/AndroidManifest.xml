<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AssetTracker">
        <activity
            android:name=".AssetCollectionActivity"
            android:exported="false"
            android:label="@string/asset_collection_title"
            android:theme="@style/Theme.AssetTracker" />
        <activity
            android:name=".AddAssetActivity"
            android:exported="false"
            android:label="@string/add_asset_title"
            android:theme="@style/Theme.AssetTracker" />
        <activity
            android:name=".EditAssetActivity"
            android:exported="false"
            android:label="@string/edit_asset_title"
            android:theme="@style/Theme.AssetTracker" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AssetTracker">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>